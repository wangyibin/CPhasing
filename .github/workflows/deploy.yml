name: Documentation

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      backfill_tags:
        description: 'Optional space-separated tags to backfill with mike, e.g. "v0.3.0 v0.3.1"'
        required: false
        default: ''
      update_docs_version:
        description: 'Publish docs from the selected ref to an existing version, e.g. "v0.3.2"'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  docs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup pixi
        uses: prefix-dev/setup-pixi@v0.8.1
        with:
          environments: docs
          cache: true

      - name: Configure Git user
        shell: bash
        run: |
          set -euo pipefail
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git fetch --tags --force
          git fetch origin gh-pages:gh-pages || true

      - name: Validate manual deployment inputs
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.backfill_tags != '' && github.event.inputs.update_docs_version != '' }}
        shell: bash
        run: |
          echo "backfill_tags and update_docs_version cannot be used together."
          exit 1

      - name: Deploy current ref with mike
        if: ${{ github.event_name != 'workflow_dispatch' || (github.event.inputs.backfill_tags == '' && github.event.inputs.update_docs_version == '') }}
        shell: bash
        run: |
          set -euo pipefail

          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
            pixi run -e docs mike deploy -F mkdocs_en.yml --push --update-aliases "$VERSION" latest
            pixi run -e docs mike deploy -F mkdocs_zh.yml --deploy-prefix zh --push --update-aliases "$VERSION" latest
            pixi run -e docs mike set-default -F mkdocs_en.yml --push latest
            pixi run -e docs mike set-default -F mkdocs_zh.yml --deploy-prefix zh --push latest

          else
            pixi run -e docs mike deploy -F mkdocs_en.yml --push --update-aliases dev 
            pixi run -e docs mike deploy -F mkdocs_zh.yml --deploy-prefix zh --push --update-aliases dev 
          fi

      - name: Update existing version with current docs
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.update_docs_version != '' }}
        shell: bash
        env:
          UPDATE_DOCS_VERSION: ${{ github.event.inputs.update_docs_version }}
        run: |
          set -euo pipefail

          VERSION="$UPDATE_DOCS_VERSION"

          if [[ "$VERSION" != v* ]] || ! git check-ref-format "refs/tags/$VERSION" >/dev/null; then
            echo "Invalid version tag: $VERSION"
            exit 1
          fi

          if ! git rev-parse -q --verify "refs/tags/$VERSION^{commit}" >/dev/null; then
            echo "Tag not found: $VERSION"
            exit 1
          fi

          latest_tag=$(git tag --list 'v*' | sort -V | tail -n 1)

          if [[ "$VERSION" == "$latest_tag" ]]; then
            pixi run -e docs mike deploy -F mkdocs_en.yml --push --update-aliases "$VERSION" latest
            pixi run -e docs mike deploy -F mkdocs_zh.yml --deploy-prefix zh --push --update-aliases "$VERSION" latest
            pixi run -e docs mike set-default -F mkdocs_en.yml --push latest
            pixi run -e docs mike set-default -F mkdocs_zh.yml --deploy-prefix zh --push latest
          else
            pixi run -e docs mike deploy -F mkdocs_en.yml --push "$VERSION"
            pixi run -e docs mike deploy -F mkdocs_zh.yml --deploy-prefix zh --push "$VERSION"
          fi

      - name: Backfill historical tags with mike
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.backfill_tags != '' }}
        shell: bash
        run: |
          set -euo pipefail

          current_ref=$(git branch --show-current)
          if [[ -z "$current_ref" ]]; then
            current_ref="main"
          fi

          latest_tag=$(git tag --list 'v*' | sort -V | tail -n 1)
          
          for tag in ${{ github.event.inputs.backfill_tags }}; do
            echo "=============================="
            echo "Backfilling $tag"
            echo "=============================="

            git rev-parse -q --verify "refs/tags/$tag" >/dev/null || {
              echo "Tag not found: $tag"
              exit 1
            }

            git checkout "refs/tags/$tag" -- docs

            if [[ "$tag" == "$latest_tag" ]]; then
              pixi run -e docs mike deploy -F mkdocs_en.yml --push --update-aliases "$tag" latest
              pixi run -e docs mike deploy -F mkdocs_zh.yml --deploy-prefix zh --push --update-aliases "$tag" latest
            else
              pixi run -e docs mike deploy -F mkdocs_en.yml --push "$tag"
              pixi run -e docs mike deploy -F mkdocs_zh.yml --deploy-prefix zh --push "$tag"
            fi
            git checkout "$current_ref" -- docs
          done
          git checkout "$current_ref" -- .
          

          if [[ -n "${latest_tag:-}" ]]; then
            pixi run -e docs mike set-default -F mkdocs_en.yml --push latest
            pixi run -e docs mike set-default -F mkdocs_zh.yml --deploy-prefix zh --push latest
          fi




# name: Documentation
# on:
#   push:
#     branches:
#       - main
# permissions:
#   contents: read
#   pages: write
#   id-token: write
# jobs:
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/configure-pages@v5
#       - uses: actions/checkout@v5
#       - uses: prefix-dev/setup-pixi@v0.8.1
#         with:
#           environments: docs
#           cache: true
#       - run: |
#           pixi run -e docs zensical build -f mkdocs_en.yml --clean 
#       - run: |
#           pixi run -e docs  zensical build -f mkdocs_zh.yml 
#       - uses: actions/upload-pages-artifact@v4
#         with:
#           path: site
#       - uses: actions/deploy-pages@v4
#         id: deployment
